[build]
  command = "npm ci --prefix client && npm run build --prefix client"
  publish = "client/dist"

[context.production.environment]
  NODE_ENV = "production"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
